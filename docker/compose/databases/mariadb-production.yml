# =============================================================================
# MariaDB — production overrides
# No port exposure. Memory limits tuned for a typical VPS.
# =============================================================================

services:
  mariadb:
    deploy:
      resources:
        limits:
          memory: 1G
        reservations:
          memory: 512M
